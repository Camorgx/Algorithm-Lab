#pragma once

#include <vector>

// k: all items in the array should be between 0 and k
extern void counting_sort(std::vector<int>& arr);

extern void merge_sort(std::vector<int>& arr);

extern void heap_sort(std::vector<int>& arr);

extern void quick_sort(std::vector<int>& arr);
